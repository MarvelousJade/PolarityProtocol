# Third-party notices

Polarity Protocol does not include imported third-party art, audio, animation, fonts, or gameplay packages.

The project uses:

- Unity Engine runtime and editor components;
- Unity built-in primitive meshes, shaders, and default resources;
- Unity Test Framework `1.6.0`;
- Unity's NUnit integration through `com.unity.ext.nunit`; and
- Visual Studio Editor package `2.0.25`.

Those components remain governed by their respective Unity and package terms. The generated Windows player contains Unity runtime binaries and must be distributed in accordance with the applicable Unity license.
